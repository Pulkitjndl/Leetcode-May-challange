class Solution {
public:
    int firstBadVersion(int n) {
        int start=0,end=n;
        int mid;
        while(start<end){

            
            mid = start+ (end-start)/2;
            if (isBadVersion(mid))
                end=mid;
            else
            start = mid+1;
        } return start;
        
    }
};